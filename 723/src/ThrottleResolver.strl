module ThrottleResolver:

input Accel : float;
input CruiseState : integer;
input Speed : float;

output UseAuto, ResetPI;

loop
    if CruiseState = 1 then
        emit UseAuto;
    else
        emit ResetPI;
    end if;
    pause;
end loop;

end module
